# Classification model for Morpheus

This repository has the classification models trained on Morpheus data and translated to real SAR data. 


For Training the classifier:

    python train.py

edit the HyperParameters.yaml file to adjust the directories and hyperparameters



For Inference:

    python inference.py